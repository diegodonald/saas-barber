// Global Jest types declaration
import '@types/jest';

declare global {
  // Jest globals are already available through @types/jest
  // This file ensures TypeScript recognizes Jest types in all test files
}

export {};
